// 核心 API 导出
export { detectOne, detectAll } from './detector';
export { initMaxMind, locate, LocateOptions } from './geolocation';

// 类型导出
export {
  Target,
  DetectResult,
  GeoResult,
  GeoSource,
  Config
} from './types';
